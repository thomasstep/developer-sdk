// import { DeveloperApp } from '../src';
import { expect } from '@jest/globals';

describe('developer-app', function () {
  it('placeholder', async function () {
    // const developerApp = DeveloperApp.init({
    //   appId: "xxx",
    //   appAuth: {
    //     mode: 'popup',
    //     accounts: {
    //       client_id: '',
    //       server_url: '',
    //     },
    //   }
    // });

    // developerApp.sdks.reporting.sendEvent({
    //   name: 'activeUser',
    // });

    // developerApp.sdks.reporting.sendEvent({
    //   name: 'error',
    //   payload: {
    //     type: '4xx',
    //   }
    // });

    // developerApp.sdks.reporting.sendEvent<{
    //   value: number;
    // }>({
    //   name: 'custom',
    //   payload: {
    //     value: 4,
    //   },
    // });

    expect(true).toBeTruthy();
  });
});
